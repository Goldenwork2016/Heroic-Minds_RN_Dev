import React from 'react'

const AudioContext = React.createContext<any>({})
const AppContext = React.createContext<any>({})
const AuthContext = React.createContext<any>({})

export { AudioContext, AppContext, AuthContext }
